export const USER_ROLES = {
    ADMIN: 'ADMIN',
    USER: 'USER',
}

export const USER_ROLES_VALUES = Object.values(USER_ROLES);

export const DELETED_USER_RETENTION_TIME = 30 * 24 * 60 * 60 * 1000; // 30 days in milliseconds